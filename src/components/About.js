import React from 'react';

export default () => {
  return (
    <section>
      <h2>About</h2>
      <p>It's a small application to show gifs using Redux-Thunk and to click pages without refreshing HTML-pages using Router</p>
    </section>
  );
}